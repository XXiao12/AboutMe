import React from 'react';
import ProjectWrapper from '../../../components/ProjectWrapper';

import './styles.css';

const LIT = (props = {}) => {
    return (
        <ProjectWrapper projectkey="LIT">
            YAY
        </ProjectWrapper>
    );
};

LIT.propTypes = {};

export default LIT;